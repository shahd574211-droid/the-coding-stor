/**
 * Supabase generated types placeholder.
 * Run: npx supabase gen types typescript --project-id YOUR_REF > src/types/supabase.ts
 * For now we use a minimal type so the server client compiles.
 */
export type Database = {
  public: Record<string, unknown>;
};
